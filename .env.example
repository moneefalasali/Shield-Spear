# Example .env for contributors - safe to commit (no secrets)
# Copy this to .env and fill in real values for local development.

SECRET_KEY=change-me
FLASK_ENV=development
DATABASE_URL=sqlite:///cybersecurity_simulator.db
REDIS_URL=redis://localhost:6379/0
SOCKETIO_MESSAGE_QUEUE=${REDIS_URL}
SENTRY_DSN=

# Public URL for the app (useful for links, redirects, OAuth callback URLs, etc.)
SITE_URL=https://shield-spear.onrender.com
